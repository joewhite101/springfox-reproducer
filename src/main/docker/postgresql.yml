version: '2'
services:
  vueblue-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/vueBlue/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=vueBlue
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
